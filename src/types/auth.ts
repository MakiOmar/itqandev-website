/**
 * Authentication-related types
 */
export type {
  User,
  AuthSession,
  LoginCredentials,
  RegisterData,
} from '../lib/auth/types';

export type { AuthAdapter } from '../lib/auth/adapters/base';
